<!--userモデルで定義したfollowing?メソッド(今ログインしているユーザーは今参照しているユーザーをフォロしているか？)-->
<% unless user == current_user %>
  <% if current_user.following?(user) %>
    <%= link_to "フォロー中", user_relationships_path(user.id), method: :delete, class: "btn btn-outline-warning btn-sm", remote: true %>
  <% else %>
    <%= link_to "フォローする", user_relationships_path(user.id), method: :post, class: "btn btn-outline-info btn-sm", remote: true %>
  <% end %>
<% end %>