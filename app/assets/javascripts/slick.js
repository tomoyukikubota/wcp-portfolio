/*global $*/
$(function() {
  $('.slider').slick({

    dots:true, default:false

  })
});


$(function() {
  $('.sliders').slick({


    autoplay:true, default:false,
    autoplaySpeed: 2000//次のスライドに切り替わる待ち時間
  })
});